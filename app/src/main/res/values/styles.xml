<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorTextMain</item>
        <item name="android:textColorPrimary">@color/colorTextMain</item>
        <item name="android:textColorSecondary">@color/colorTextMain</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--delete later-->
    <style name="AppTheme.PreferenceStyle">
        <item name="android:textColor">@color/colorTextMain</item>
        <item name="android:textColorPrimary">@color/colorTextMain</item>
        <item name="android:textColorSecondary">@color/colorTextMain</item>
    </style>

</resources>
